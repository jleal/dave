<%= form_for(@funky_book) do |f| %>
  <% if @funky_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funky_book.errors.count, "error") %> prohibited this funky_book from being saved:</h2>

      <ul>
      <% @funky_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
